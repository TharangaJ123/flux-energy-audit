{
  "name": "flux-energy-audit",
  "version": "1.0.0",
  "description": "Root package to manage backend and frontend testing",
  "scripts": {
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "test": "npm run test:backend && npm run test:frontend",
    "perf:carbon": "cd backend && npx artillery run tests/performance/carbon-tracker-load.yml"
  }
}
