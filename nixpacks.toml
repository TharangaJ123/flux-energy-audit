[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install", "cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npm install"]

[start]
cmd = "cd backend && npm start"
